const saveCharacterSaves = function() {
  // check loaded characters if there is a character matching this character
  fetch()
  // once saved, update the load menu
  // update the global variable characterId
}